{
  "name": "animal-adoption-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/backend"
  ],
  "scripts": {
    "dev:backend": "npm run dev -w packages/backend",
    "build": "npm run build -w packages/backend",
    "test": "npm test -w packages/backend",
    "type-check": "npm run type-check -w packages/shared && npm run type-check -w packages/backend"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
